{
  "name": "slime-mold",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "pretest": "tsc --noEmit false --outDir .test-dist",
    "test": "node --test \"src/**/__tests__/*.test.mjs\" \"tests/*.test.mjs\"",
    "verify": "npm run pretest && node scripts/verify.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "fzstd": "^0.1.1"
  }
}
