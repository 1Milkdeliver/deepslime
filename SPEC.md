# 黏菌(Slime Mold)——任务记忆体 SPEC v0.1

> 状态:草稿(待评审签字)
> 定位:跨 agent、跨会话、跨时间的任务级持久记忆系统
> 核心承诺:**会话可弃,记忆永存**

---

## 1. 产品定位

### 1.1 一句话

黏菌是一个以"任务"为中心、跨 agent、跨会话、跨时间共享的持久记忆系统:你随时可以开启任意新的会话/新的 agent,它自动接上这个任务的过往记忆;你随时可以丢弃会话,记忆不丢。

### 1.2 卖点层级(定死,不得混淆)

| 层级 | 内容 | 角色 |
|---|---|---|
| 场景 | 多 agent 丝滑切换 | 定义用户是谁(重度多 agent 用户),用于传播故事 |
| **承诺** | **会话可弃,记忆永存** | 定义用户为什么留下(损失厌恶、焦虑解除),唯一卖点 |
| 副产品 | 井井有条的感觉 | 定义体验(检索+溯源带来的"一问就到"),**绝不展示为"自动分类"** |

### 1.3 目标用户

重度多 agent 用户:同时在 DSH、Claude Code、ChatGPT、Cursor 等之间切换,经常"忘了哪个 agent 哪个对话做了哪件事"。

### 1.4 产品形态(身份定死)

- **本质身份:一个以"服务"为形态的应用**(类比:文件系统,不是记事本);
- 出生方式:插件(DSH Cordis 原型,验证用);
- 长大方式:桌面应用(本地守护进程 + MCP + Obsidian 作为浏览器层);
- 扩张方式:SaaS(团队/公司层,个人记忆永不强制上云);
- 内容:skill 生态(共享"方法",永不共享"经历");
- **明确不是 agent**(不做 agent 就意味着不和 agent 竞争,记忆不绑定单个执行体);
- **开发协作:Codex CLI 协同开发/评审 + GitHub 公开仓库(开源)**。

### 1.5 开源策略

- 项目以 **MIT 协议开源**,仓库托管于 GitHub(账号:1Milkdeliver);
- 开源兑现"社区养分"模式:共享**方法与模板**(skill、schema、流程),永不共享**个人经历数据**;
- 个人记忆数据永远留在用户本地(开源的是代码,不是数据);
- 开源带来:免费审查、社区插件、可信度(记忆系统必须能被审计——闭源记忆系统没人敢信)。

---

## 2. 核心概念

### 2.1 任务为主键

- 记忆的组织单位是**任务**,不是 agent、对话、文件、时间;
- agent/会话/软件/文件夹/文档/URL 全部是任务的**刻面(属性)**,不是平行分类轴;
- 任务身份:uuid + 人类可读名称。

### 2.2 会话无状态化

- 会话是一次性壳,记忆是唯一持久层;
- 用户可随时丢弃、新开、换 agent,因为身份和状态不在会话里,在任务记忆里。

### 2.3 双记忆(分开存储,捕获机制不同)

| 类型 | 内容 | 来源 |
|---|---|---|
| 陈述记忆 | 事实、产物、报告、决策、偏好 | 产物文件 + 对话摘要 |
| 程序记忆 | 流程、skill、工具调用序列、参数 | 执行环境工具调用日志 |

- **产物优先于对话**:报告本身 > 产生它的对话;
- 程序记忆第一版 = skill 库 + 工具调用日志索引,不发明新东西。

### 2.4 生命周期状态机

```
[休眠 dormant] --task.open--> [活跃 active] --记录→ checkpoint→ handoff--
      ^                                        |
      |                                    task.close
      |                                        v
      +--- 相似任务苏醒(检索召回) <------- [萎缩 compacted]
```

- 开任务 → 附着 → 记录 → 关任务 → 萎缩(压缩归档,非破坏性)→ 相似任务苏醒 → 接续;
- 萎缩/苏醒由**用户行为**(任务开/关)驱动,不是由 token 预算驱动;
- 压缩必须非破坏性:原始日志归档,摘要只是投影。

### 2.5 记忆分级

```
任务原型(如"周报"): 模板 / 格式偏好 / 反馈史 / 常用语气   ← 每次必加载,量小稳定
任务实例(2024-11 第3周): 历次产物 / 当时用的agent / 数据来源  ← 按需检索 top-k
片段(对话记录): 压缩后的事件摘要                          ← 只在回溯时展开
```

- "自我学习"只发生在原型层(从反馈中提炼偏好),实例层只增长不学习。

---

## 3. 落地场景

### 3.1 场景一:周报日报(验证跨会话聚合 + 重回主题)

- 用户用不同 agent 多次做周报,之后新开会话,零成本接上全部历史;
- 歪题后,一条指令(`task.reattach`)随时重回主题,不用翻记录。

### 3.2 场景二:简历修改(验证长跨度 + 记忆比人可靠)

- 隔几年再改简历:agent 沿过往路径检索所有工作经历(文档、周报、汇报、旧简历、旧 AI 对话);
- 用完即归档,记忆不消失;
- **红线:简历任务只准读"事实层",禁止读"草稿层"**(草稿/计划/讨论不得当作实绩)。

### 3.3 场景三:跨境电商(验证程序记忆——从回忆到执行)

- 新项目自动调用旧记忆:复用邮件 skill、广告策略、多维表格、工具与流程;
- **执行纪律:盲重放必死**——执行前校验参数是否仍成立(价格、联系人、平台规则会漂移)。

---

## 4. 养分来源(记忆从哪来)

| 模式 | 内容 | 定位 |
|---|---|---|
| 主动投喂 | 用户主动喂链接/文件/资料/图片 | **MVP 唯一依赖,信噪比最高** |
| 自我学习 | 任务锚定期间的事件捕获(文档/URL/工具),非无差别录屏 | 必需但做窄 |
| 社区进化 | 开源社区维护软件 + 共享"方法与模板" | 共享方法,永不共享个人经历 |

- **红线:社区层传"方法",不传"经历"**;
- 无差别全盘捕获(录屏式)= 不做(隐私 + 成本 + 无法展示缺口);
- "它自己进化"翻译成人话 = "检索质量随使用变好",不是"它自己决定学什么"。

---

## 5. 架构

### 5.1 形态:三件套(Obsidian 承担浏览器层)

```
本地守护进程(常驻,唯一主人)
  ├── 任务区域存储:一个 Obsidian vault(markdown/JSON,人可读,git 可 diff)
  │     └── 存储与 Obsidian 解耦:纯 markdown 文件夹,Obsidian 只是浏览器,不是依赖
  ├── MCP 服务(协议层——任何 agent 都能读写)
  └── 浏览器层:Obsidian 本体(图谱视图 = 任务菌落地图) + 溯源插件(自建,小)
```

- **选择 Obsidian 的理由**:任务区域本来就是人可读 markdown;Obsidian 的图谱视图天然是"任务菌落"可视化;搜索/双链/标签免费获得;**省掉自建面板的最大 UI 成本**;
- **依赖纪律**:存储格式是纯 markdown,Obsidian 可随时被替换(将来不用 Obsidian,数据零迁移成本);
- **需要自建的只有**:溯源栏(记忆来自哪个 agent/会话/时间/置信度)→ 一个小的 Obsidian 自定义视图插件;
- 本地嵌入模型(SQLite 向量索引)由守护进程管理,与 Obsidian 无关。

### 5.2 任务区域目录结构(位于 Obsidian vault 内)

```
<vault>/
├── slime-mold/
│   ├── tasks/<task-id>/
│   │   ├── task.json        # 身份+生命周期元数据
│   │   ├── state.md         # 接续简报: 当前状态/已完成/未完成/约束/下一步  ← 接续第一入口
│   │   ├── log/             # 时序记忆条目(原始,不删)
│   │   ├── artifacts/       # 产物文件
│   │   └── index/           # 嵌入索引(可由守护进程管理)
│   ├── skills/              # 程序记忆:skill 库(共享"方法"的载体)
│   └── sm-config.json       # 守护进程配置(嵌入模型、授权档位等)
```

### 5.3 MCP 工具集(七个)

| 工具 | 语义 | 对应生命周期 |
|---|---|---|
| `task.open(name\|id)` | 加载 state.md + 最近条目 + 相似任务召回 | 苏醒 |
| `task.record(entry)` | 结构化写入 log(强制带来源) | 附着记录 |
| `task.checkpoint(state)` | 更新 state.md | 状态固化 |
| `task.handoff(brief)` | 生成/更新接续简报 | 冷启动接手 |
| `task.close()` | 压缩 + 归档 + 休眠 | 萎缩 |
| `task.search(query)` | 全区域检索 | 触角探寻 |
| `task.reattach()` | 重新锚定到当前任务 + 拉回记忆 | 重回主题 |

### 5.4 记忆条目 schema(log entry)

```json
{
  "id": "uuid",
  "task_id": "uuid",
  "agent": "claude-code|dsh|chatgpt|cursor",
  "session_id": "...",
  "timestamp": "ISO8601",
  "type": "decision|artifact|observation|question|fact",
  "layer": "fact|draft",          // 事实层 vs 草稿层,简历只读 fact
  "kind": "reference|state",      // 参考记忆(可自动加载) vs 状态记忆(恢复前确认)
  "summary": "一句话摘要",
  "payload_ref": "路径或为空",     // 产物引用,原文不内联
  "confidence": "high|medium|low",
  "source_scope": "personal|team|company"  // 个人层恒为 personal
}
```

### 5.5 加载协议(防误加载)

- **两级加载 + 可见加载**:
  1. 第一级只加载摘要/索引(几十 token):"已定位 18 次周报记忆,来自 3 个 agent,含 1 套模板、2 次格式反馈";
  2. 第二级在确认或明确需要时展开细节;
  3. **面板必须显示"已加载哪些来源"**——可见 = 可纠正 = 可信。
- 意图检测三层:显式(用户说"周报")→ 隐式(窗口标题/当前文档)→ 建议兜底(agent 询问),**不依赖纯自动判断**。

---

## 6. 信任与安全

### 6.1 溯源(provenance)是安全机制,不是 UI 点缀

- 每条记忆必带 {agent, session, timestamp, type, confidence};
- 面板能回答:"这段记忆来自 Claude 会话 X,2024-11-03,置信度中,类型为推测"。

### 6.2 覆盖诚实(承诺"我记得我抓到的",不是"我记得一切")

- 召回时同步展示来源 + **缺口**("2021 年在 X 工具的记录未接入");
- 缺失要显式标注,禁止把"缺失"当成"没有"。

### 6.3 冲突处理

- 两个 agent 给出矛盾结论 → 保留双方 + 时间戳,不自动覆盖;
- 恢复"状态记忆"前必须向用户确认(用户可能已改需求)。

### 6.4 分档授权

| 档位 | 范围 | 策略 |
|---|---|---|
| 读取 | 记忆检索、加载 | 免费 |
| 内部写入 | 建表格、写草稿、改文件 | 沙箱 |
| 外部不可逆动作 | 发邮件、花钱、对外发布 | **逐次审批 + dry-run 预览** |

---

## 7. 评测指标(没有指标 = 自嗨)

| 指标 | 定义 | 目标 |
|---|---|---|
| 接续时间 | 同一任务,有记忆 vs 无记忆,到有效接续的耗时比 | 显著下降 |
| 误加载率 | 相似但不相关任务被错误加载的比例 | < 5% |
| 重回成功率 | 歪题后一条指令回到有效工作状态的比例 | > 90% |
| 记忆复用率 | 新任务命中旧任务记忆的比例 | 持续上升 |
| 溯源透明度 | 用户能指出任一记忆条目来源的比例 | ≈ 100% |

---

## 8. 开发计划与预算

### 8.1 阶段划分

| 阶段 | 由谁 | 内容 | 验收门 |
|---|---|---|---|
| SPEC(本阶段) | 主会话+1 子代理 | 固化架构 | 用户签字 |
| 编码 | **主会话 + Codex 协同** | Cordis 插件:任务区域+7工具+Obsidian 溯源插件 | 最小闭环跑通 |
| 调试 | 主会话 + Codex 评审 | 诊断修复 + 代码审查 | 三项指标出数字 |
| 子代理并行 | 子代理×4-6 + Codex | 研究/文档/审查/测试语料 | 审查意见并入 |

### 8.2 开发协作分工(Codex + GitHub)

- **主会话负责**:架构决策、Cordis 插件核心逻辑、工具行为、溯源插件设计——所有涉及"为什么这么设计"的部分;
- **Codex CLI 负责**:机械性编码、批量生成测试用例、代码审查(独立视角抓主会话盲区)、重构执行——通过 `codex exec` 后台运行,产出由主会话审查合并;
- **GitHub 负责**:版本控制 + 开源发布,`gh repo create` 建公开仓库,MIT 协议;
- **纪律**:Codex 的产出必须经主会话审查后才能进主分支——它不共享本会话的架构上下文,只能依据 SPEC 和代码工作;SPEC 冲突时以 SPEC 为准。

### 8.2 预算(2026-08-17 新峰谷定价,元/百万 tokens)

- V4-Flash:空闲 输入缓存命中 ¥0.05 / 未命中 ¥1.5 / 输出 ¥4.5;高峰(9:00、14:00)翻倍;
- 开发期总预算:**空闲 ¥25 封顶 / 高峰 ¥50 封顶**(含 20% 缓冲)。

### 8.3 省钱机制(结构性)

1. SPEC 即共享记忆——避免每轮重新解释;
2. 核心代码不外包——子代理写核心 = 多付一遍上下文搬运费;
3. 错峰调度——批处理(压缩、索引)放夜间,价格减半;
4. 小步快跑——一次 define 只加一个能力,失败定位快;
5. 缓存友好——同一任务反复加载走缓存命中价(¥0.05/M)。

### 8.4 止损规则

- 编码超 40 轮没过最小闭环 → 停,回 SPEC 砍范围(先砍自动捕获,只留手动锚点);
- 调试超 30 轮指标没出 → 停,可能是设计问题不是 bug,回设计阶段;
- 子代理研究结果与 SPEC 冲突 → 以 SPEC 为准,差异记录为 v2 事项。

---

## 9. 路线图(尺度演进)

| 阶段 | 目标 | 周期 |
|---|---|---|
| P0 | 个人 MVP(场景一+二),DSH Cordis 插件 | 2-4 周 |
| P1 | 个人完整层:桌面应用(守护进程+MCP+面板),双记忆、生命周期、自动捕获(任务锚定版)、多 agent 插件 | 数月 |
| P2 | 团队层:跨人接续(同事菌落可接入,用户设置共享范围) | 半年+ |
| P3 | 公司楔子:跨人任务状态接续(Bob 离职,Alice 的 agent 接手)——区别于 Glean 的唯一楔子 | 更长 |
| 不做 | 互联网"可写记忆" | 永远(安全红线) |

---

## 10. 非目标(明确不做什么)

- ❌ 不是 agent(不竞争执行,只提供记忆);
- ❌ 不是聊天应用(不提供对话);
- ❌ 不是笔记软件(笔记是产物不是主体);
- ❌ 不是录屏工具(只捕获任务锚定期间事件);
- ❌ 不是企业 KM 平台(P3 只是扩展楔子);
- ❌ 不做互联网可写记忆;
- ❌ 不做无差别全盘深度摘要(成本+隐私);
- ❌ 不展示"自动分类"为卖点(只以检索+溯源面貌呈现"井井有条")。

---

## 11. 风险登记

| 风险 | 等级 | 缓解 |
|---|---|---|
| 任务边界检测不准 | 高 | 手动锚点 + 自动建议,不依赖纯自动 |
| 外来 agent 记忆写入质量不可控 | 高 | schema 固定 + 原始日志保留 + handoff 契约 |
| 错误记忆被当真(幻觉传染) | 高 | 溯源 + 置信度 + 覆盖诚实 + 事实层隔离 |
| schema 漂移(旧记忆不可读) | 中 | 版本化存储,旧记忆永远可读 |
| 第三方依赖(LinkedIn/GitHub ToS) | 中 | 优先接入自有数据(文档/周报/汇报/旧简历) |
| 多设备同步 | 中 | P1 本地优先 + 可选同步,个人记忆永不强制上云 |

---

## 12. 待决问题(Open Questions)

1. Obsidian 本地未安装 —— 需要先安装 Obsidian(免费),或先用纯 markdown 开发、Obsidian 仅作为可选浏览器层(推荐后者,保持解耦);
2. 多设备同步方案(P1 前必须定:Obsidian Sync / iCloud / NAS / 仅单机?);
3. 程序记忆捕获的最小集(哪些工具调用值得记录?);
4. 任务原型自动提炼的触发条件(多少次反馈算"偏好"?);
5. 溯源插件的承载形式(Obsidian 自定义视图 vs 独立 webview)。

---

*本文档是后续所有开发阶段的唯一事实源。修改必须走版本记录,禁止口头改架构。*
